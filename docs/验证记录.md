# 本次验证

版本：1.1.0。

已通过：

- 桌面端、官网的 TypeScript 检查和生产构建。
- 43 项自动化测试通过，覆盖 Key 加密、25 个请求直接并发派发、临时失败自动重试最多 2 次、余额/Key 错误不重试、Retry-After 等待要求、超时、失败缓存清除、长请求去重、错误原因展示、翻译、回复目的/角色、1—4 条建议、图片消息和发送方关联、截图边界、切换会话中止、官方余额格式、聊天标记持久化、无客户档案、更新链接和密码复制。
- 真实 Electron 窗口启动：首次显示 Key 配置和教程，账户、应用中心、代理页面正常，无渲染错误、无横向溢出，不再出现“客户数据”菜单。
- 真实 Electron 沙箱 preload 与模拟聊天图片：同源图片转码、跨域图片局部捕获、图片发送方识别、切换聊天拒绝读取全部通过。此测试使用合成图，不访问真实用户账号或调用付费 API。
- 官网 1440px 桌面、390px 手机尺寸检查：无溢出、无图片加载失败；阻止所有外部资源请求后仍可正常加载本地图标与字体；手机菜单可开关。
- Windows 安装包构建。软件与快捷方式使用原名称，不额外加版本类别后缀。

尚未验证或未执行：

- 没有填入真实用户的 API Key，实际计费调用、余额数值和模型输出质量需用户在软件内测试。
- 未操作用户的真实聊天账号；平台重新登录、在线翻译、真实照片识图效果、AI 插入草稿和标签显示需实际试用确认。
- 安装包尚未上传蓝奏云，官网尚未替换线上版本，旧后端与数据库未停用或删除。
- 项目所有者已选择 GPL-3.0-only；第三方许可文本已收集，源码经过常见凭证模式检查。发布结果以 GitHub 仓库和 Releases 页面为准。

原商业工程保留供回退。本次只移除新版中的商业功能和客户档案功能，没有删除旧用户数据。
