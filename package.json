{
  "name": "seagrass-standalone",
  "version": "1.1.0",
  "private": true,
  "license": "GPL-3.0-only",
  "description": "本地保存数据、使用自有 DeepSeek API Key 的海草跨境助手",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm --dir desktop dev",
    "build": "pnpm --dir desktop build && pnpm --dir website build",
    "typecheck": "pnpm --dir desktop exec vue-tsc --noEmit && pnpm --dir website exec vue-tsc --noEmit",
    "test": "node --test desktop/electron/*.test.cjs",
    "package:win": "pnpm --dir desktop package:win"
  }
}
